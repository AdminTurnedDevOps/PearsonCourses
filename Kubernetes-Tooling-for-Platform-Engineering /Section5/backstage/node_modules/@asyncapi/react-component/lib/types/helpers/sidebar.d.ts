import { TagsInterface } from '@asyncapi/parser';
export interface TagObject<T = any> {
    name: string;
    tags: TagsInterface;
    data: T;
}
export interface SortedReturnType {
    tagged: Map<string, TagObject[]>;
    untagged: TagObject[];
}
export declare function filterObjectsByTags<T>(tags: string[], objects: TagObject<T>[]): SortedReturnType;
//# sourceMappingURL=sidebar.d.ts.map