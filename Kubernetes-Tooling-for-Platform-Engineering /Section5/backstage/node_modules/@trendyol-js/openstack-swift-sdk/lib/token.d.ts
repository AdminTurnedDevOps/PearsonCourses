export declare class Token {
    private readonly token;
    private readonly expiresAt;
    private readonly expireAtMillisecond;
    constructor(token: string, expiresAt: Date);
    isExpired(): boolean;
    getAsHeader(): any;
    toString: () => string;
}
