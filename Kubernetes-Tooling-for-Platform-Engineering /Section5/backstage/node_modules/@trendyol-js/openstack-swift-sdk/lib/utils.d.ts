 
export declare class UrlBuilder {
    private basePath;
    private queryStrings;
    constructor(basePath: string);
    addQueryString(q: string, v: any): UrlBuilder;
    addQueryStringIf(q: string, v: any, condition?: (val: any, type: string) => boolean): UrlBuilder;
    build(): string;
}
