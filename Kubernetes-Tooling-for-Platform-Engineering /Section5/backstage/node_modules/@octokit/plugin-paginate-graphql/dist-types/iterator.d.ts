import type { Octokit } from "@octokit/core";
declare const createIterator: (octokit: Octokit) => <ResponseType_1 = any>(query: string, initialParameters?: Record<string, any>) => {
    [Symbol.asyncIterator]: () => {
        next(): Promise<{
            done: boolean;
            value: ResponseType_1;
        }>;
    };
};
export { createIterator };
