export type QueueMethods<T> = {
    items: T[];
    first: T | undefined;
    last: T | undefined;
    add: (item: T) => void;
    remove: () => T;
    size: number;
};
export declare function useQueue<T>(initialValue?: T[]): QueueMethods<T>;
