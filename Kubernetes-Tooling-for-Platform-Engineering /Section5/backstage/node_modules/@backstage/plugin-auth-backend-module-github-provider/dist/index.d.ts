import * as _backstage_plugin_auth_node from '@backstage/plugin-auth-node';
import { PassportOAuthAuthenticatorHelper, PassportProfile, OAuthAuthenticatorResult } from '@backstage/plugin-auth-node';
import * as _backstage_backend_plugin_api from '@backstage/backend-plugin-api';

/** @public */
declare const githubAuthenticator: _backstage_plugin_auth_node.OAuthAuthenticator<PassportOAuthAuthenticatorHelper, PassportProfile>;

/** @public */
declare const authModuleGithubProvider: _backstage_backend_plugin_api.BackendFeature;

/**
 * Available sign-in resolvers for the GitHub auth provider.
 *
 * @public
 */
declare namespace githubSignInResolvers {
    /**
     * Looks up the user by matching their GitHub username to the entity name.
     */
    const usernameMatchingUserEntityName: _backstage_plugin_auth_node.SignInResolverFactory<OAuthAuthenticatorResult<PassportProfile>, unknown>;
}

export { authModuleGithubProvider as default, githubAuthenticator, githubSignInResolvers };
