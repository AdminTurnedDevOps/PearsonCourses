'use strict';

var errors = require('@backstage/errors');
var common = require('./common.cjs.js');

function parseEntityFacetParams(params) {
  const facetStrings = common.parseStringsParam(params.facet, "facet");
  if (facetStrings) {
    const filtered = facetStrings.filter(Boolean);
    if (filtered.length) {
      return filtered;
    }
  }
  throw new errors.InputError("Missing facet parameter");
}

exports.parseEntityFacetParams = parseEntityFacetParams;
//# sourceMappingURL=parseEntityFacetParams.cjs.js.map
