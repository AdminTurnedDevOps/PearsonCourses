'use strict';

var helpers = require('../helpers.cjs.js');
var config = require('./config.cjs.js');

class AwsS3Integration {
  constructor(integrationConfig) {
    this.integrationConfig = integrationConfig;
  }
  static factory = ({ config: config$1 }) => {
    const configs = config.readAwsS3IntegrationConfigs(
      config$1.getOptionalConfigArray("integrations.awsS3") ?? []
    );
    return helpers.basicIntegrations(
      configs.map((c) => new AwsS3Integration(c)),
      (i) => i.config.host
    );
  };
  get type() {
    return "awsS3";
  }
  get title() {
    return this.integrationConfig.host;
  }
  get config() {
    return this.integrationConfig;
  }
  resolveUrl(options) {
    const resolved = helpers.defaultScmResolveUrl(options);
    return resolved;
  }
  resolveEditUrl(url) {
    return url;
  }
}

exports.AwsS3Integration = AwsS3Integration;
//# sourceMappingURL=AwsS3Integration.cjs.js.map
