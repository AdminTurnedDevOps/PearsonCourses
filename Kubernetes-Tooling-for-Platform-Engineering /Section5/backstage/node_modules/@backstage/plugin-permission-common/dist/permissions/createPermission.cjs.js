'use strict';

function createPermission({
  name,
  attributes,
  resourceType
}) {
  if (resourceType) {
    return {
      type: "resource",
      name,
      attributes,
      resourceType
    };
  }
  return {
    type: "basic",
    name,
    attributes
  };
}

exports.createPermission = createPermission;
//# sourceMappingURL=createPermission.cjs.js.map
