import { makeStyles } from '@material-ui/core/styles';
import React__default from 'react';
import { IconLinkVertical } from './IconLinkVertical.esm.js';

const useStyles = makeStyles(
  (theme) => ({
    links: {
      margin: theme.spacing(2, 0),
      display: "grid",
      gridAutoFlow: "column",
      gridAutoColumns: "min-content",
      gridGap: theme.spacing(3)
    }
  }),
  { name: "BackstageHeaderIconLinkRow" }
);
function HeaderIconLinkRow(props) {
  const { links } = props;
  const classes = useStyles();
  return /* @__PURE__ */ React__default.createElement("nav", { className: classes.links }, links.map((link, index) => /* @__PURE__ */ React__default.createElement(IconLinkVertical, { key: index + 1, ...link })));
}

export { HeaderIconLinkRow };
//# sourceMappingURL=HeaderIconLinkRow.esm.js.map
