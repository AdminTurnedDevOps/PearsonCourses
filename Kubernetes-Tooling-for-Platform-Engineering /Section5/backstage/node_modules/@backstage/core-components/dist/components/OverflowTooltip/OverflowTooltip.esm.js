import { makeStyles } from '@material-ui/core/styles';
import Tooltip from '@material-ui/core/Tooltip';
import React__default from 'react';
import Typography from '@material-ui/core/Typography';

const useStyles = makeStyles(
  {
    container: {
      overflow: "visible !important"
    },
    typo: {
      fontSize: "inherit",
      overflow: "hidden",
      textOverflow: "ellipsis",
      display: "-webkit-box",
      "-webkit-line-clamp": ({ line }) => line || 1,
      "-webkit-box-orient": "vertical"
    }
  },
  { name: "BackstageOverflowTooltip" }
);
function OverflowTooltip(props) {
  const classes = useStyles(props);
  return /* @__PURE__ */ React__default.createElement(
    Tooltip,
    {
      title: props.title ?? (props.text || ""),
      placement: props.placement
    },
    /* @__PURE__ */ React__default.createElement(Typography, { className: classes.typo, variant: "inherit" }, props.text)
  );
}

export { OverflowTooltip };
//# sourceMappingURL=OverflowTooltip.esm.js.map
