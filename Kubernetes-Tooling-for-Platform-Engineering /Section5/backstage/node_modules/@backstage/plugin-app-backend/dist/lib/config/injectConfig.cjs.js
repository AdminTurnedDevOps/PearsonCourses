'use strict';

var injectConfigIntoHtml = require('./injectConfigIntoHtml.cjs.js');
var injectConfigIntoStatic = require('./injectConfigIntoStatic.cjs.js');

async function injectConfig(options) {
  const indexHtmlContent = await injectConfigIntoHtml.injectConfigIntoHtml(options);
  if (indexHtmlContent) {
    return { indexHtmlContent };
  }
  const injectedPath = await injectConfigIntoStatic.injectConfigIntoStatic(options);
  return { injectedPath };
}

exports.injectConfig = injectConfig;
//# sourceMappingURL=injectConfig.cjs.js.map
