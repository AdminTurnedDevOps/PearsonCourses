'use strict';

var backendPluginApi = require('@backstage/backend-plugin-api');
var pluginAuthNode = require('@backstage/plugin-auth-node');
var authenticator = require('./authenticator.cjs.js');
var resolvers = require('./resolvers.cjs.js');

const authModuleOauth2Provider = backendPluginApi.createBackendModule({
  pluginId: "auth",
  moduleId: "oauth2-provider",
  register(reg) {
    reg.registerInit({
      deps: {
        providers: pluginAuthNode.authProvidersExtensionPoint
      },
      async init({ providers }) {
        providers.registerProvider({
          providerId: "oauth2",
          factory: pluginAuthNode.createOAuthProviderFactory({
            authenticator: authenticator.oauth2Authenticator,
            signInResolverFactories: {
              ...resolvers.oauth2SignInResolvers,
              ...pluginAuthNode.commonSignInResolvers
            }
          })
        });
      }
    });
  }
});

exports.authModuleOauth2Provider = authModuleOauth2Provider;
//# sourceMappingURL=module.cjs.js.map
