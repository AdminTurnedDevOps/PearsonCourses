import entityEnvelopeSchema from '../schema/EntityEnvelope.schema.json.esm.js';
import { compileAjvSchema, throwAjvError } from './ajv.esm.js';

function entityEnvelopeSchemaValidator(schema) {
  const validate = compileAjvSchema(
    schema ? schema : entityEnvelopeSchema
  );
  return (data) => {
    const result = validate(data);
    if (result === true) {
      return data;
    }
    throw throwAjvError(validate.errors);
  };
}

export { entityEnvelopeSchemaValidator };
//# sourceMappingURL=entityEnvelopeSchemaValidator.esm.js.map
