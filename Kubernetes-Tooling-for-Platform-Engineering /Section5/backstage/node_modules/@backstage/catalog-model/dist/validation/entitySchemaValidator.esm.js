import entitySchema from '../schema/Entity.schema.json.esm.js';
import { compileAjvSchema, throwAjvError } from './ajv.esm.js';

function entitySchemaValidator(schema) {
  const validate = compileAjvSchema(schema ? schema : entitySchema);
  return (data) => {
    const result = validate(data);
    if (result === true) {
      return data;
    }
    throw throwAjvError(validate.errors);
  };
}

export { entitySchemaValidator };
//# sourceMappingURL=entitySchemaValidator.esm.js.map
