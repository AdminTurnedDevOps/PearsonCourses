'use strict';

const createConditionFactory = (rule) => {
  return (params) => {
    return {
      rule: rule.name,
      resourceType: rule.resourceType,
      params
    };
  };
};

exports.createConditionFactory = createConditionFactory;
//# sourceMappingURL=createConditionFactory.cjs.js.map
