// Constants are pretty much the same thing as variables, but they don't change.
// Variables can change, constants are hard-coded

// Why use constants? If you need a hard-coded value that you know will never change

package main

import "fmt"

func main() {
	const hiagain = "hello world"
	fmt.Println(hiagain)
}
